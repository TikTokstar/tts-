# Bulgarian TTS bot за TikTok Live — зависимости
# Всичко е CPU-only. Никакви невронни модели локално, никакъв torch.

# --- Синтез на реч ---------------------------------------------------------
# Безплатните неврални гласове на Microsoft. Онлайн, но бърз.
edge-tts>=7.0,<8.0

# --- TikTok чат ------------------------------------------------------------
TikTokLive>=6.5,<7.0

# --- Звук ------------------------------------------------------------------
# sounddevice носи PortAudio вътре в колелото за Windows — не искa нищо ръчно.
sounddevice>=0.4.6
# Декодира mp3 без ffmpeg. Малко C колело, не е ML.
miniaudio>=1.59
# Резервен декодер, ако miniaudio не се справи. Иска ffmpeg в PATH.
pydub>=0.25.1

# --- Контролен панел -------------------------------------------------------
fastapi>=0.110
uvicorn>=0.27
jinja2>=3.1
python-multipart>=0.0.9
websockets>=12.0

# --- Разни -----------------------------------------------------------------
python-dotenv>=1.0
# Глобални клавишни комбинации, които работят и докато си в игра.
pynput>=1.7.6

# --- Тестове ---------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23
